// Service Worker para permitir el funcionamiento de la PWA
self.addEventListener('fetch', function(event) {
    // Por ahora, solo cumple con el requisito de instalación
    // En el futuro, aquí añadiremos el código para que la app funcione sin internet
});
